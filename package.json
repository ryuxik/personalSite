{
  "name": "ryuxik-io",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "photo-meta": "node scripts/photo-meta.mjs",
    "og-images": "node scripts/og-images.mjs",
    "generate": "npm run photo-meta && npm run og-images",
    "predev": "npm run generate",
    "dev": "astro dev",
    "prebuild": "npm run generate",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "placeholders": "node scripts/make-placeholders.mjs",
    "check-hdr": "node scripts/check-hdr.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource-variable/newsreader": "^5.3.0",
    "astro": "^7.2.1",
    "exifreader": "^4.42.0",
    "sharp": "^0.35.3",
    "thumbhash": "^0.1.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
